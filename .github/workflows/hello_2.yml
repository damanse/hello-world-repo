name: Hello World 2
on: 
 workflow_dispatch:
   inputs:
     name:
       description: enter name
       
#  schedule: 
#    - cron: '40 * * * *'
jobs:
  hello_job:
    runs-on: ubuntu-latest
    steps:
      - name: Echo Text
        run: echo "{{ Hello, inputs.name 2.... }}"
